import { LoginForm } from '@/components/auth/LoginForm';

export default function LoginPage() {
  return (
    <main className="items-center justify-center bg-gradient-to-br from-blue-50 to-indigo-50 text-black ">
      <LoginForm />
    </main>
  );
}
